class Account {
    constructor(id, accountNumber, balance, currency, customerId) {
      this.id = id;
      this.accountNumber = accountNumber;
      this.balance = balance;
      this.currency = currency;
      this.customerId = customerId;
    }
  }
  